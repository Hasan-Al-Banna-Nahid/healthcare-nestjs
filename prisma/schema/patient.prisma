model Patient {
    id        String   @id @default(uuid(7))
    name      String
    email     String   @unique
    password  String?
    phone     String?
    address   String?
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("patient")
}
